# ============================================================================
# CONFIGURAÇÃO DO FAZ.PY - Processador de Livros KDP
# ============================================================================
# Copie este arquivo para .env e configure suas credenciais

# --- BACKEND DE IA ---
# Opções: "ollama" (local/gratuito), "gemini", "openai"
MODEL_BACKEND=ollama

# --- OLLAMA (Modelo Local) ---
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:7b

# Modelos recomendados para português:
# - qwen2.5:7b      (6GB VRAM) - Excelente multilíngue, recomendado
# - qwen2.5:14b     (10GB VRAM) - Melhor qualidade, mais lento
# - llama3.1:8b     (8GB VRAM) - Bom em português
# - mistral:7b      (6GB VRAM) - Razoável em português
# - gemma2:9b       (10GB VRAM) - Bom, do Google

# --- GEMINI API (Google) ---
GOOGLE_API_KEY=sua_chave_aqui

# --- OPENAI API ---
OPENAI_API_KEY=sua_chave_aqui

# --- CONFIGURAÇÕES DE PROCESSAMENTO ---
MAX_CHUNK_TOKENS=1500
MAX_OUTPUT_TOKENS=4096
TEMPERATURE=0.3
MAX_RETRIES=5
RETRY_BASE_WAIT=5
PARALLEL_CHUNKS=3

# --- DIRETÓRIOS ---
INPUT_TXT_DIR=txt
OUTPUT_DOCX_DIR=docx
OUTPUT_TXT_DIR=txt
TEMPLATE_DOCX=Estrutura.docx

# --- EMAIL (Opcional) ---
EMAIL_SENDER_ADDRESS=
EMAIL_SENDER_APP_PASSWORD=
EMAIL_RECIPIENT_ADDRESS=
EMAIL_SMTP_SERVER=smtp.gmail.com
EMAIL_SMTP_PORT=587
